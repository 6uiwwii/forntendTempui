<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Setting Page</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@400;700&display=swap" rel="stylesheet">
    
    <style>

        body, html {
            margin: 0;
            padding: 0;
            font-family: 'Kanit', sans-serif;
            background-color: #f0f0f0;
        }

        .nav-page {
            width: 375px;
            min-height: 812px;
            margin: 20px auto;
            background-color: #5F8FAA;
            border: 1px solid #ccc;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            overflow: hidden;
        }

        .page-header {
            height: 50px;
            background-color: #74503E;
            width: 100%;
            display: flex;
            align-items: center;
            padding: 0 15px;
            box-sizing: border-box;
        }
        .header-icons-wrapper {
            margin-left: auto;
            display: flex;
            gap: 8px;
        }
        .icon-block {
            width: 25px;
            height: 25px;
            background-color: #D9D9D9;
            border-radius: 5px;
        }

        .page-content {
            padding: 20px;
            box-sizing: border-box;
            text-align: center;
        }

        .setting-title {
            color: white;
            font-size: 3em; 
            font-weight: bold;
            margin-top: 30px;
            margin-bottom: 10px;
            position: relative;
        }
        
        .setting-title::after {
            content: '';
            display: block;
            width: 100px; 
            height: 3px;  
            background-color: white;
            border-radius: 2px;
            margin: 10px auto 0;
        }

        .setting-menu {
            margin-top: 40px; 
        }


        .setting-item {
            background-color: white;
            border-radius: 10px;
            padding: 15px 20px;
            margin: 0 auto 20px auto;
            max-width: 300px; 
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            
            color: #4A4A4A;
            font-size: 1.2em;
            font-weight: bold;
            text-decoration: none;
            
            display: flex;
            align-items: center;
        }


        .setting-item.is-button {
            justify-content: center;
            cursor: pointer;
            transition: background-color 0.2s;
        }
        .setting-item.is-button:hover {
            background-color: #f9f9f9; 
        }

        .setting-item.is-toggle {
            justify-content: space-between; 
        }

        .toggle-switch {
            position: relative;
            display: inline-block;
            width: 50px;
            height: 28px;
        }
        
        .toggle-switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }

        .toggle-slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: #ccc;
            border-radius: 28px;
            transition: .4s;
        }
        
        .toggle-slider:before {
            position: absolute;
            content: "";
            height: 20px;
            width: 20px;
            left: 4px;
            bottom: 4px;
            background-color: white;
            border-radius: 50%;
            transition: .4s;
        }

        input:checked + .toggle-slider {
            background-color: #5F8FAA;
        }
        input:checked + .toggle-slider:before {
            transform: translateX(22px);
        }

    </style>
</head>
<body>

    <div class="nav-page">
        
        <header class="page-header">
            <div class="header-icons-wrapper">
                <div class="icon-block"></div>
                <label class="toggle-switch">
                        <input type="checkbox" checked> <span class="toggle-slider"></span>
                </label>
            </div>
        </header>

        <main class="page-content">

            <h1 class="setting-title">Setting</h1>

            <div class="setting-menu">
                
                <div class="setting-item is-toggle">
                    <span>การแจ้งเตือน</span>
                    
                    <label class="toggle-switch">
                        <input type="checkbox" checked> <span class="toggle-slider"></span>
                    </label>
                </div>

                <a href="#" class="setting-item is-button">
                    <span>เปลี่ยน Board</span>
                </a>

                <a href="#" class="setting-item is-button">
                    <span>ออกจากระบบ</span>
                </a>

            </div>

        </main>
    </div>

</body>
</html>